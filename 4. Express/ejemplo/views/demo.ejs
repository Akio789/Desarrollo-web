<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ejemplo</title>
</head>

<body>
    <p>
        fact(<%= n %>) = <%= x %>
    </p>
    <ul>
        <% for (let enano of enanos) { %>
        <% if (enano !== 'Bombur') { %>
        <li><%= enano %> </li>
        <% } %>
        <% } %>
    </ul>
</body>

</html>